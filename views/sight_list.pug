extends layout 

block content

    h1= title 

    ul 
        //- sight_list.sort(function(a , b) { let textA = a.name.toUpperCase(); let textB = b.name.toUpperCase(); return(textA < textB) ? -1: (textA > textB) ? 1 : 0;});
        each sight in sight_list 
            li
                a(href=sight.url) #{sight.features[0].properties.name} 
                | (#{sight.features[0].properties.link})
                | (#{sight.features[0].properties.description})
                
        else 
            li There are no sights.